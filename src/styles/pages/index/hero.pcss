.section-hero {
	padding-top: 50px;
	padding-bottom: 50px;
}

.section-hero__central {
	width: var(--section-width);
	min-width: 320px;
	max-width: 1920px;
	margin: 0 auto;
	padding: var(--section-padding);
}
